 foreach($products as $product =>$key){
        if($key["category"] === $category){
            array_push($array, $key);
        }
       
    }

    if(count($array) === 0){
        array_push($array, $cat_error);
        }
    
        if(empty($show)){
            echo (json_encode($array));
        }
        else{
            $array2 = [];
            for($i = 0; $i < $show; $i++){
                array_push($array2, $array[$i])
            }
        }
    
    
    
}